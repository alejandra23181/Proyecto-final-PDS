spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          url: file://
  security:
    user:
      name: admin
      password: admin
  server:
    port: 9000
